pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh './gradlew clean build spotlessCheck test jib publish'
            }
            post {
                always {
                    archiveArtifacts 'build/libs/*.jar'
                }
            }
        }
    }
}