pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh './gradlew clean build test publish'
            }
            post {
                always {
                    archiveArtifacts 'build/libs/*.jar'
                }
            }
        }
    }
}