pipeline {
    agent any
    stages {
        stage('Build') {
            steps {
                sh './gradlew clean build spotlessCheck test publish'
            }
            post {
                always {
                    archiveArtifacts 'build/libs/*.jar'
                }
            }
        }
    }
}